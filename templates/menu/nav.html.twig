<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-white mt-5">
    <!-- Container wrapper -->
    <div class="container-fluid">

        <!-- Collapsible wrapper -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <!-- Navbar brand -->
            <a class="navbar-brand mt-2 mt-lg-0" href="#">
                <img src="{{ asset('img/icon.png') }}" alt="Sorties !"
                     class="rounded-circle"
                     height="50"
                     alt="Black and White Portrait of a Man"
                     loading="lazy">
            </a>
            <!-- Left links -->
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}" title="Logout">Se déconnecter {{ app.user.nom | capitalize }} {{ app.user.prenom | capitalize }}</a> </li>
                {% else %}
                    <li class="nav-item"><a class="nav-link"href="{{ path('app_login') }}" title="Login">Se connecter</a></li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="#">Dashboard</a>
                </li>
            </ul>
            <!-- Left links -->
        </div>
        <!-- Collapsible wrapper -->

        <!-- Right elements -->
        <div class="d-flex align-items-center">
            <!-- Icon -->
            <a class="link-secondary me-3" href="#">
                <i class="fas fa-shopping-cart"></i>
            </a>


            <!-- Avatar -->


                    <img
                            src="{{ asset('img/backdrops/' ~ app.user.backdrop)}}" alt="lol"
                            class="rounded-circle"
                            height="25"
                            alt="Black and White Portrait of a Man"
                            loading="lazy"
                    />

        </div>
        <!-- Right elements -->
    </div>
    <!-- Container wrapper -->
</nav>
<!-- Navbar -->