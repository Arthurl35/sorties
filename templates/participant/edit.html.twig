{% extends 'base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <h1>Modifier un participant</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_participant_index') }}"><button class="btn btn-secondary">Retour</button></a>

    {{ include('participant/_delete_form.html.twig') }}
{% endblock %}
